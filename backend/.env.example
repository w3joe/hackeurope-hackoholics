# Gemini API key (required for Module 2, Orchestration)
# Get one from https://ai.google.dev/gemini-api/docs/api-key
GOOGLE_API_KEY=your-gemini-api-key
# Alternative env var (fallback)
# GEMINI_API_KEY=your-gemini-api-key

# Crusoe API key (required for Module 1B)
# Get one from your HackEurope Crusoe project
CRUSOE_API_KEY=your-crusoe-api-key
# Optional overrides:
# CRUSOE_BASE_URL=https://hackeurope.crusoecloud.com/v1/
# CRUSOE_MODEL=NVFP4/Qwen3-235B-A22B-Instruct-2507-FP4

# Module 2: Pharmacy Inventory Gap Analyzer (batches run concurrently)
# MODULE_2_MAX_PHARMACIES=20  # Max pharmacies (0 = all). Default 20 to avoid rate-limit stalls.
# MODULE_2_BATCH_SIZE=5       # Pharmacies per LLM call. Default 5 (avoids truncation).
# MODULE_2_REQUEST_TIMEOUT=120  # Seconds per LLM call before abort. Default 120.
# MODULE_2_BATCH_DELAY=5     # Seconds between batches (avoid rate limit). Default 5.

# Orchestration: same batch size (default 5)
# ORCHESTRATION_BATCH_SIZE=5

# Alerts / Supabase Realtime (push notifications to frontend)
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_SERVICE_KEY=your-service-role-key
# ALERTS_API_KEY=your-secret-key-for-internal-alerts-endpoint
