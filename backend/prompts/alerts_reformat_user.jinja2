Convert these risk assessments into operator alerts. Return a JSON object with an "items" array. Each item must have "description" and "severity" (one of: "low", "watch", "urgent"). Same order as input.

Risk assessments (JSON):
{{ risk_assessments_json }}
