# MacOS-compatible requirements (CPU only, no bitsandbytes)

# Core ML libraries
torch>=2.0.0
transformers>=4.36.0
peft>=0.7.0
accelerate>=0.25.0
trl>=0.7.4

# Data processing
datasets>=2.18.0
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0

# Utilities
tqdm>=4.65.0
